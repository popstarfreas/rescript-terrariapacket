// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

let Belt_Option = require("@rescript/runtime/lib/js/Belt_Option.js");
let BitFlags$TerrariaPacket = require("../../BitFlags.js");
let PacketType$TerrariaPacket = require("../../PacketType.js");
let ManagedPacketWriter$PacketFactory = require("@popstarfreas/packetfactory/src/ManagedPacketWriter.js");
let Packetreader = require("@popstarfreas/packetfactory/packetreader").default;
let Packetwriter = require("@popstarfreas/packetfactory/packetwriter").default;

function readSingle(prim) {
  return prim.readSingle();
}

function readInt16(prim) {
  return prim.readInt16();
}

function readByte(prim) {
  return prim.readByte();
}

function parse(payload) {
  let reader = new Packetreader(payload);
  let projectileId = reader.readInt16();
  let x = reader.readSingle();
  let y = reader.readSingle();
  let vx = reader.readSingle();
  let vy = reader.readSingle();
  let owner = reader.readByte();
  let projectileType = reader.readInt16();
  let flags = BitFlags$TerrariaPacket.fromByte(reader.readByte());
  let ai_0 = BitFlags$TerrariaPacket.flag1(flags) ? reader.readSingle() : undefined;
  let ai_1 = BitFlags$TerrariaPacket.flag2(flags) ? reader.readSingle() : undefined;
  let ai = [
    ai_0,
    ai_1,
    undefined
  ];
  let damage = BitFlags$TerrariaPacket.flag5(flags) ? reader.readInt16() : undefined;
  let knockback = BitFlags$TerrariaPacket.flag6(flags) ? reader.readSingle() : undefined;
  let originalDamage = BitFlags$TerrariaPacket.flag7(flags) ? reader.readInt16() : undefined;
  let projectileUuid = BitFlags$TerrariaPacket.flag8(flags) ? reader.readInt16() : undefined;
  return {
    projectileId: projectileId,
    x: x,
    y: y,
    vx: vx,
    vy: vy,
    owner: owner,
    projectileType: projectileType,
    ai: ai,
    bannerIdToRespondTo: undefined,
    damage: damage,
    knockback: knockback,
    originalDamage: originalDamage,
    projectileUuid: projectileUuid
  };
}

let Decode = {
  readSingle: readSingle,
  readInt16: readInt16,
  readByte: readByte,
  parse: parse
};

function packSingle(prim0, prim1) {
  return prim0.packSingle(prim1);
}

function packInt16(prim0, prim1) {
  return prim0.packInt16(prim1);
}

function packByte(prim0, prim1) {
  return prim0.packByte(prim1);
}

function data(prim) {
  return prim.data;
}

function packOptionalData(writer, self) {
  let match = self.ai;
  let ai1 = match[1];
  let ai0 = match[0];
  let bitFlags = BitFlags$TerrariaPacket.fromFlags(Belt_Option.isSome(ai0), Belt_Option.isSome(ai1), false, false, Belt_Option.isSome(self.damage), Belt_Option.isSome(self.knockback), Belt_Option.isSome(self.originalDamage), Belt_Option.isSome(self.projectileUuid));
  writer.packByte(BitFlags$TerrariaPacket.toByte(bitFlags));
  if (BitFlags$TerrariaPacket.flag1(bitFlags)) {
    writer.packSingle(ai0);
  }
  if (BitFlags$TerrariaPacket.flag2(bitFlags)) {
    writer.packSingle(ai1);
  }
  if (BitFlags$TerrariaPacket.flag5(bitFlags)) {
    writer.packInt16(self.damage);
  }
  if (BitFlags$TerrariaPacket.flag6(bitFlags)) {
    writer.packSingle(self.knockback);
  }
  if (BitFlags$TerrariaPacket.flag7(bitFlags)) {
    writer.packInt16(self.originalDamage);
  }
  if (BitFlags$TerrariaPacket.flag8(bitFlags)) {
    writer.packInt16(self.projectileUuid);
  }
  return writer;
}

function toBuffer(self) {
  let id = self.projectileId;
  return packOptionalData(ManagedPacketWriter$PacketFactory.setType(new Packetwriter(), PacketType$TerrariaPacket.toInt("ProjectileSync")).packInt16(id === 954 ? 504 : (
      id === 955 ? 12 : id
    )).packSingle(self.x).packSingle(self.y).packSingle(self.vx).packSingle(self.vy).packByte(self.owner).packInt16(self.projectileType), self).data;
}

let Encode = {
  packSingle: packSingle,
  packInt16: packInt16,
  packByte: packByte,
  setType: ManagedPacketWriter$PacketFactory.setType,
  data: data,
  packOptionalData: packOptionalData,
  starCannonStarProjectileId: 955,
  fallenStarProjectileId: 12,
  wandOfSparkingSparkProjectileId: 954,
  sparkProjectileId: 504,
  toBuffer: toBuffer
};

exports.Decode = Decode;
exports.Encode = Encode;
exports.parse = parse;
exports.toBuffer = toBuffer;
/* @popstarfreas/packetfactory/packetreader Not a pure module */
