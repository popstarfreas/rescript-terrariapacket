// Generated by ReScript, PLEASE EDIT WITH CARE

import * as PacketType$TerrariaPacket from "../PacketType.js";
import * as ErrorAwarePacketReader$TerrariaPacket from "../ErrorAwarePacketReader.js";
import * as ErrorAwarePacketWriter$TerrariaPacket from "../ErrorAwarePacketWriter.js";
import Packetreader from "@popstarfreas/packetfactory/packetreader";

function parse(payload) {
  let reader = new Packetreader(payload);
  let e = ErrorAwarePacketReader$TerrariaPacket.readInt32(reader, "id");
  if (e.TAG !== "Ok") {
    return e;
  }
  let id = e._0;
  let e$1 = ErrorAwarePacketReader$TerrariaPacket.readByte(reader, "anchorType");
  if (e$1.TAG !== "Ok") {
    return e$1;
  }
  let anchorType = e$1._0;
  if (anchorType === 255) {
    return {
      TAG: "Ok",
      _0: {
        id: id,
        anchor: "Remove"
      }
    };
  }
  let e$2 = ErrorAwarePacketReader$TerrariaPacket.readUInt16(reader, "anchorMeta");
  if (e$2.TAG !== "Ok") {
    return e$2;
  }
  let anchorMeta = e$2._0;
  let e$3 = ErrorAwarePacketReader$TerrariaPacket.readUInt16(reader, "time");
  if (e$3.TAG !== "Ok") {
    return e$3;
  }
  let time = e$3._0;
  let e$4 = ErrorAwarePacketReader$TerrariaPacket.readByte(reader, "emote");
  if (e$4.TAG !== "Ok") {
    return e$4;
  }
  let emoteByte = e$4._0;
  if (emoteByte !== 255) {
    return {
      TAG: "Ok",
      _0: {
        id: id,
        anchor: {
          TAG: "Anchor",
          anchorType: anchorType,
          anchorMeta: anchorMeta,
          time: time,
          emote: emoteByte,
          metadata: undefined
        }
      }
    };
  }
  let e$5 = ErrorAwarePacketReader$TerrariaPacket.readInt16(reader, "metadata");
  if (e$5.TAG === "Ok") {
    return {
      TAG: "Ok",
      _0: {
        id: id,
        anchor: {
          TAG: "Anchor",
          anchorType: anchorType,
          anchorMeta: anchorMeta,
          time: time,
          emote: -1,
          metadata: e$5._0
        }
      }
    };
  } else {
    return e$5;
  }
}

function toBuffer(self) {
  let writer = ErrorAwarePacketWriter$TerrariaPacket.packInt32(ErrorAwarePacketWriter$TerrariaPacket.setType(ErrorAwarePacketWriter$TerrariaPacket.make(), PacketType$TerrariaPacket.toInt("EmoteBubble")), self.id, "id");
  let details = self.anchor;
  if (typeof details !== "object") {
    return ErrorAwarePacketWriter$TerrariaPacket.data(ErrorAwarePacketWriter$TerrariaPacket.packByte(writer, 255, "anchorType"));
  }
  let emoteByte = details.emote === -1 ? 255 : details.emote;
  let writer$1 = ErrorAwarePacketWriter$TerrariaPacket.packByte(ErrorAwarePacketWriter$TerrariaPacket.packUInt16(ErrorAwarePacketWriter$TerrariaPacket.packUInt16(ErrorAwarePacketWriter$TerrariaPacket.packByte(writer, details.anchorType, "anchorType"), details.anchorMeta, "anchorMeta"), details.time, "time"), emoteByte, "emote");
  let tmp;
  if (details.emote === -1) {
    let v = details.metadata;
    tmp = ErrorAwarePacketWriter$TerrariaPacket.packInt16(writer$1, v !== undefined ? v : 0, "metadata");
  } else {
    tmp = writer$1;
  }
  return ErrorAwarePacketWriter$TerrariaPacket.data(tmp);
}

export {
  parse,
  toBuffer,
}
/* ErrorAwarePacketWriter-TerrariaPacket Not a pure module */
