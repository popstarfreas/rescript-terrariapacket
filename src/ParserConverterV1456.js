// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";
import * as Parser$TerrariaPacket from "./Parser.js";
import * as PacketType$TerrariaPacket from "./PacketType.js";
import * as ParserV1456$TerrariaPacket from "./ParserV1456.js";
import * as PacketTypeV1456$TerrariaPacket from "./PacketTypeV1456.js";

function ownershipFromV1456(value) {
  let match = value & 3;
  switch (match) {
    case 0 :
      return "None";
    case 1 :
      return "ReserveForLocalPlayer";
    case 2 :
      return "GrabDelayForLocalPlayer";
    default:
      return "GrabDelayForAllPlayers";
  }
}

function ownershipToV1456(ownership) {
  switch (ownership) {
    case "None" :
      return 0;
    case "ReserveForLocalPlayer" :
      return 1;
    case "GrabDelayForLocalPlayer" :
      return 2;
    case "GrabDelayForAllPlayers" :
      return 3;
  }
}

function itemDropFromV1456(item, shimmer, enemyGrabDelayTime) {
  return {
    itemDropId: item.itemDropId,
    position: {
      x: item.x,
      y: item.y
    },
    velocity: {
      x: item.vx,
      y: item.vy
    },
    stack: item.stack,
    prefix: item.prefix,
    ownership: ownershipFromV1456(item.noDelay),
    itemId: item.itemId,
    shimmer: shimmer,
    enemyGrabDelayTime: enemyGrabDelayTime
  };
}

function itemDropBaseToV1456(item) {
  return {
    itemDropId: item.itemDropId,
    x: item.position.x,
    y: item.position.y,
    vx: item.velocity.x,
    vy: item.velocity.y,
    stack: item.stack,
    prefix: item.prefix,
    noDelay: ownershipToV1456(item.ownership),
    itemId: item.itemId
  };
}

function netModuleToLatest(value) {
  switch (value.TAG) {
    case "Liquid" :
      return {
        TAG: "Liquid",
        _0: value._0
      };
    case "ClientText" :
      return {
        TAG: "ClientText",
        _0: value._0,
        _1: value._1
      };
    case "ServerText" :
      return {
        TAG: "ServerText",
        _0: value._0,
        _1: value._1,
        _2: value._2
      };
    case "Ping" :
      return {
        TAG: "Ping",
        _0: value._0
      };
    case "Ambience" :
      return {
        TAG: "Ambience",
        _0: value._0
      };
    case "Bestiary" :
      return {
        TAG: "Bestiary",
        _0: value._0
      };
    case "CreativePower" :
      return {
        TAG: "CreativePower",
        _0: value._0
      };
    case "CreativeUnlocksPlayerReport" :
      return {
        TAG: "CreativeUnlocksPlayerReport",
        _0: value._0
      };
    case "TeleportPylon" :
      return {
        TAG: "TeleportPylon",
        _0: value._0
      };
    case "Particles" :
      return {
        TAG: "Particles",
        _0: value._0
      };
    case "CreativePowerPermissions" :
      return {
        TAG: "CreativePowerPermissions",
        _0: value._0
      };
    case "Banners" :
      return {
        TAG: "Banners",
        _0: value._0
      };
    case "CraftingRequests" :
      return {
        TAG: "CraftingRequests",
        _0: value._0
      };
    case "TagEffectState" :
      return;
    case "LeashedEntity" :
      return {
        TAG: "LeashedEntity",
        _0: value._0
      };
    case "UnbreakableWallScan" :
      return {
        TAG: "UnbreakableWallScan",
        _0: value._0
      };
  }
}

function netModuleFromLatest(value) {
  switch (value.TAG) {
    case "Liquid" :
      return {
        TAG: "Liquid",
        _0: value._0
      };
    case "ClientText" :
      return {
        TAG: "ClientText",
        _0: value._0,
        _1: value._1
      };
    case "ServerText" :
      return {
        TAG: "ServerText",
        _0: value._0,
        _1: value._1,
        _2: value._2
      };
    case "Ping" :
      return {
        TAG: "Ping",
        _0: value._0
      };
    case "Ambience" :
      return {
        TAG: "Ambience",
        _0: value._0
      };
    case "Bestiary" :
      return {
        TAG: "Bestiary",
        _0: value._0
      };
    case "CreativeUnlocks" :
      let payload = value._0;
      return {
        TAG: "CreativeUnlocksPlayerReport",
        _0: {
          userId: 0,
          itemId: payload.itemId,
          researchedCount: payload.sacrificeCount
        }
      };
    case "CreativePower" :
      return {
        TAG: "CreativePower",
        _0: value._0
      };
    case "CreativeUnlocksPlayerReport" :
      return {
        TAG: "CreativeUnlocksPlayerReport",
        _0: value._0
      };
    case "TeleportPylon" :
      return {
        TAG: "TeleportPylon",
        _0: value._0
      };
    case "Particles" :
      return {
        TAG: "Particles",
        _0: value._0
      };
    case "CreativePowerPermissions" :
      return {
        TAG: "CreativePowerPermissions",
        _0: value._0
      };
    case "Banners" :
      return {
        TAG: "Banners",
        _0: value._0
      };
    case "CraftingRequests" :
      return {
        TAG: "CraftingRequests",
        _0: value._0
      };
    case "LeashedEntity" :
      return {
        TAG: "LeashedEntity",
        _0: value._0
      };
    case "UnbreakableWallScan" :
      return {
        TAG: "UnbreakableWallScan",
        _0: value._0
      };
  }
}

function toLatest(packet) {
  switch (packet.TAG) {
    case "WorldInfo" :
      let newrecord = {...packet._0};
      return {
        TAG: "WorldInfo",
        _0: (newrecord.dungeonY = 0, newrecord.dungeonX = 0, newrecord.noLightningSeed = false, newrecord.moreLightningSeed = false, newrecord.forceChristmasForever = false, newrecord.forceHalloweenForever = false, newrecord)
      };
    case "PlayerUpdate" :
      let value = packet._0;
      let controlBase = value.control;
      let newrecord$1 = {...value};
      return {
        TAG: "PlayerUpdate",
        _0: (newrecord$1.snappingStoneLightUp = false, newrecord$1.control = {
          isHoldingUp: controlBase.isHoldingUp,
          isHoldingDown: controlBase.isHoldingDown,
          isHoldingLeft: controlBase.isHoldingLeft,
          isHoldingRight: controlBase.isHoldingRight,
          isHoldingJump: controlBase.isHoldingJump,
          isHoldingItemUse: controlBase.isHoldingItemUse,
          isHoldingDash: false
        }, newrecord$1)
      };
    case "ItemDropUpdate" :
      return {
        TAG: "ItemDropUpdate",
        _0: itemDropFromV1456(packet._0, undefined, undefined)
      };
    case "ItemOwner" :
      let value$1 = packet._0;
      return {
        TAG: "ItemOwner",
        _0: {
          itemDropId: value$1.itemDropId,
          owner: value$1.owner,
          timeToKeepReservation: 0,
          grabDelayPlayer: 0,
          grabDelayTime: 0,
          position: value$1.position
        }
      };
    case "NpcUpdate" :
      let value$2 = packet._0;
      return {
        TAG: "NpcUpdate",
        _0: {
          npcSlotId: value$2.npcSlotId,
          generation: 0,
          npcTypeId: value$2.npcTypeId,
          x: value$2.x,
          y: value$2.y,
          vx: value$2.vx,
          vy: value$2.vy,
          target: value$2.target,
          directionX: value$2.directionX,
          directionY: value$2.directionY,
          ai: value$2.ai,
          spriteDirection: value$2.spriteDirection,
          life: value$2.life,
          releaseOwner: value$2.releaseOwner,
          playerCountScale: value$2.playerCountScale,
          difficulty: value$2.difficulty,
          spawnedFromStatue: value$2.spawnedFromStatue,
          spawnNeedsSyncing: value$2.spawnNeedsSyncing,
          shimmerTransparency: value$2.shimmerTransparency
        }
      };
    case "ProjectileSync" :
      let value$3 = packet._0;
      return {
        TAG: "ProjectileSync",
        _0: {
          projectileKey: {
            spawner: value$3.owner,
            index: value$3.projectileId,
            generation: Stdlib_Option.getOr(value$3.projectileUuid, 0)
          },
          position: {
            x: value$3.x,
            y: value$3.y
          },
          velocity: {
            x: value$3.vx,
            y: value$3.vy
          },
          projectileType: value$3.projectileType,
          ai: value$3.ai,
          bannerIdToRespondTo: value$3.bannerIdToRespondTo,
          damage: value$3.damage,
          knockback: value$3.knockback,
          originalDamage: value$3.originalDamage
        }
      };
    case "NpcStrike" :
      let value$4 = packet._0;
      return {
        TAG: "NpcStrike",
        _0: {
          npcSlotId: value$4.npcId,
          generation: 0,
          damage: value$4.damage,
          knockback: value$4.knockback,
          direction: value$4.direction,
          critical: value$4.critical
        }
      };
    case "ProjectileDestroy" :
      let value$5 = packet._0;
      return {
        TAG: "ProjectileDestroy",
        _0: {
          projectileKey: {
            spawner: value$5.owner,
            index: value$5.projectileId,
            generation: 0
          },
          position: {
            x: 0.0,
            y: 0.0
          }
        }
      };
    case "ItemOwnerRemove" :
      return {
        TAG: "ItemOwnerRemove",
        _0: {
          itemDropId: packet._0.itemDropId,
          forceAssignToServer: false
        }
      };
    case "PlayerDodge" :
      let value$6 = packet._0;
      let match = value$6.dodge;
      let tmp;
      switch (match) {
        case "Ninja" :
          tmp = "Ninja";
          break;
        case "Shadow" :
          tmp = "Shadow";
          break;
        case "BrainOfConfusion" :
          tmp = "BrainOfConfusion";
          break;
      }
      return {
        TAG: "PlayerDodge",
        _0: {
          playerId: value$6.playerId,
          dodge: tmp
        }
      };
    case "NpcCatch" :
      return {
        TAG: "NpcCatch",
        _0: {
          npcId: packet._0.npcId
        }
      };
    case "NetModuleLoad" :
      return Stdlib_Option.map(netModuleToLatest(packet._0), value => ({
        TAG: "NetModuleLoad",
        _0: value
      }));
    case "ItemDropInstancedUpdate" :
      return {
        TAG: "ItemDropInstancedUpdate",
        _0: itemDropFromV1456(packet._0, undefined, undefined)
      };
    case "ItemDropShimmeredUpdate" :
      let value$7 = packet._0;
      return {
        TAG: "ItemDropUpdate",
        _0: itemDropFromV1456(value$7, {
          shimmered: value$7.shimmered,
          shimmerTime: value$7.shimmeredTime
        }, undefined)
      };
    case "ItemDropProtectedUpdate" :
      let value$8 = packet._0;
      return {
        TAG: "ItemDropUpdate",
        _0: itemDropFromV1456(value$8, undefined, value$8.timeLeftInWhichTheItemCannotBeTakenByEnemies)
      };
    case "ServerInfo" :
      return {
        TAG: "ServerInfo",
        _0: undefined
      };
    case "PlayerPlatformInfo" :
      return {
        TAG: "PlayerPlatformInfo",
        _0: packet._0
      };
    default:
      return;
  }
}

function fromLatest(packet) {
  switch (packet.TAG) {
    case "WorldInfo" :
      return {
        TAG: "WorldInfo",
        _0: packet._0
      };
    case "PlayerUpdate" :
      return {
        TAG: "PlayerUpdate",
        _0: packet._0
      };
    case "ItemDropUpdate" :
      let value = packet._0;
      let match = value.shimmer;
      let match$1 = value.enemyGrabDelayTime;
      if (match !== undefined) {
        let base = itemDropBaseToV1456(value);
        return {
          TAG: "ItemDropShimmeredUpdate",
          _0: {
            itemDropId: base.itemDropId,
            x: base.x,
            y: base.y,
            vx: base.vx,
            vy: base.vy,
            stack: base.stack,
            prefix: base.prefix,
            noDelay: base.noDelay,
            itemId: base.itemId,
            shimmered: match.shimmered,
            shimmeredTime: match.shimmerTime
          }
        };
      }
      if (match$1 === undefined) {
        return {
          TAG: "ItemDropUpdate",
          _0: itemDropBaseToV1456(value)
        };
      }
      let base$1 = itemDropBaseToV1456(value);
      return {
        TAG: "ItemDropProtectedUpdate",
        _0: {
          itemDropId: base$1.itemDropId,
          x: base$1.x,
          y: base$1.y,
          vx: base$1.vx,
          vy: base$1.vy,
          stack: base$1.stack,
          prefix: base$1.prefix,
          noDelay: base$1.noDelay,
          itemId: base$1.itemId,
          timeLeftInWhichTheItemCannotBeTakenByEnemies: match$1
        }
      };
    case "ItemOwner" :
      return {
        TAG: "ItemOwner",
        _0: packet._0
      };
    case "NpcUpdate" :
      return {
        TAG: "NpcUpdate",
        _0: packet._0
      };
    case "ProjectileSync" :
      let value$1 = packet._0;
      return {
        TAG: "ProjectileSync",
        _0: {
          projectileId: value$1.projectileKey.index,
          x: value$1.position.x,
          y: value$1.position.y,
          vx: value$1.velocity.x,
          vy: value$1.velocity.y,
          owner: value$1.projectileKey.spawner,
          projectileType: value$1.projectileType,
          ai: value$1.ai,
          bannerIdToRespondTo: value$1.bannerIdToRespondTo,
          damage: value$1.damage,
          knockback: value$1.knockback,
          originalDamage: value$1.originalDamage,
          projectileUuid: value$1.projectileKey.generation === 0 ? undefined : value$1.projectileKey.generation
        }
      };
    case "NpcStrike" :
      let value$2 = packet._0;
      return {
        TAG: "NpcStrike",
        _0: {
          npcId: value$2.npcSlotId,
          damage: value$2.damage,
          knockback: value$2.knockback,
          direction: value$2.direction,
          critical: value$2.critical
        }
      };
    case "ProjectileDestroy" :
      let value$3 = packet._0;
      return {
        TAG: "ProjectileDestroy",
        _0: {
          projectileId: value$3.projectileKey.index,
          owner: value$3.projectileKey.spawner
        }
      };
    case "ItemOwnerRemove" :
      return {
        TAG: "ItemOwnerRemove",
        _0: {
          itemDropId: packet._0.itemDropId
        }
      };
    case "PlayerDodge" :
      let value$4 = packet._0;
      let match$2 = value$4.dodge;
      let tmp;
      switch (match$2) {
        case "Ninja" :
          tmp = "Ninja";
          break;
        case "Shadow" :
          tmp = "Shadow";
          break;
        case "BrainOfConfusion" :
        case "MysticSash" :
          tmp = "BrainOfConfusion";
          break;
      }
      return {
        TAG: "PlayerDodge",
        _0: {
          playerId: value$4.playerId,
          dodge: tmp
        }
      };
    case "NpcCatch" :
      return {
        TAG: "NpcCatch",
        _0: {
          npcId: packet._0.npcId,
          playerId: 0
        }
      };
    case "NetModuleLoad" :
      return {
        TAG: "NetModuleLoad",
        _0: netModuleFromLatest(packet._0)
      };
    case "ItemDropInstancedUpdate" :
      return {
        TAG: "ItemDropInstancedUpdate",
        _0: itemDropBaseToV1456(packet._0)
      };
    case "ServerInfo" :
      return {
        TAG: "ServerInfo",
        _0: undefined
      };
    case "PlayerPlatformInfo" :
      return {
        TAG: "PlayerPlatformInfo",
        _0: packet._0
      };
    default:
      return packet;
  }
}

function convertToLatestIfNeeded(buffer, fromServer) {
  if (buffer.length < 3) {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketLength",
        _0: buffer.length
      }
    };
  }
  let match = PacketTypeV1456$TerrariaPacket.fromInt(buffer[2]);
  if (match === undefined) {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketType",
        _0: buffer[2]
      }
    };
  }
  switch (match) {
    case "NpcItemStrike" :
      return {
        TAG: "Ok",
        _0: "DiscardAsNotExists"
      };
    case "CavernMonsterTypeSync" :
    case "ShimmerEffectOrCoinLuck" :
      if (fromServer) {
        return {
          TAG: "Ok",
          _0: "PacketStructureIsSame"
        };
      } else {
        return {
          TAG: "Ok",
          _0: "DiscardAsNotExists"
        };
      }
    case "WorldInfo" :
    case "PlayerUpdate" :
    case "ItemDropUpdate" :
    case "ItemOwner" :
    case "NpcUpdate" :
    case "ProjectileSync" :
    case "NpcStrike" :
    case "ProjectileDestroy" :
    case "ItemOwnerRemove" :
    case "PlayerDodge" :
    case "NpcCatch" :
    case "NetModuleLoad" :
    case "ItemDropInstancedUpdate" :
    case "ItemDropShimmeredUpdate" :
    case "ItemDropProtectedUpdate" :
    case "ServerInfo" :
    case "PlayerPlatformInfo" :
      break;
    default:
      return {
        TAG: "Ok",
        _0: "PacketStructureIsSame"
      };
  }
  return Stdlib_Result.map(ParserV1456$TerrariaPacket.parse(buffer, fromServer, undefined), packet => {
    let packet$1 = toLatest(packet);
    if (packet$1 !== undefined) {
      return {
        TAG: "ConvertedToLatest",
        _0: packet$1
      };
    } else {
      return "DiscardAsNotExists";
    }
  });
}

function convertFromLatestIfNeeded(buffer, fromServer) {
  if (buffer.length < 3) {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketLength",
        _0: buffer.length
      }
    };
  }
  let match = PacketType$TerrariaPacket.fromInt(buffer[2]);
  if (match === undefined) {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketType",
        _0: buffer[2]
      }
    };
  }
  switch (match) {
    case "NpcItemStrike" :
    case "ItemDropShimmeredUpdate" :
    case "ItemDropProtectedUpdate" :
    case "DamageNPCAck" :
      return {
        TAG: "Ok",
        _0: "DiscardAsNotExists"
      };
    case "WorldInfo" :
    case "PlayerUpdate" :
    case "ItemDropUpdate" :
    case "ItemOwner" :
    case "NpcUpdate" :
    case "ProjectileSync" :
    case "NpcStrike" :
    case "ProjectileDestroy" :
    case "ItemOwnerRemove" :
    case "PlayerDodge" :
    case "NpcCatch" :
    case "NetModuleLoad" :
    case "ItemDropInstancedUpdate" :
    case "ServerInfo" :
    case "PlayerPlatformInfo" :
      break;
    default:
      return {
        TAG: "Ok",
        _0: "PacketStructureIsSame"
      };
  }
  return Stdlib_Result.map(Parser$TerrariaPacket.parse(buffer, fromServer, undefined), packet => {
    if (packet.TAG === "NetModuleLoad" && packet._0.TAG === "CreativeUnlocks") {
      return "DiscardAsNotExists";
    } else {
      return {
        TAG: "ConvertedFromLatest",
        _0: fromLatest(packet)
      };
    }
  });
}

export {
  convertToLatestIfNeeded,
  convertFromLatestIfNeeded,
}
/* Parser-TerrariaPacket Not a pure module */
