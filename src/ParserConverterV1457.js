// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";
import * as Parser$TerrariaPacket from "./Parser.js";
import * as PacketType$TerrariaPacket from "./PacketType.js";
import * as ParserV1457$TerrariaPacket from "./ParserV1457.js";
import * as PacketTypeV1457$TerrariaPacket from "./PacketTypeV1457.js";

function convertToLatestIfNeeded(buffer, fromServer) {
  if (buffer.length < 3) {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketLength",
        _0: buffer.length
      }
    };
  }
  let match = PacketTypeV1457$TerrariaPacket.fromInt(buffer[2]);
  if (match === undefined) {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketType",
        _0: buffer[2]
      }
    };
  }
  switch (match) {
    case "WorldInfo" :
      return Stdlib_Result.map(ParserV1457$TerrariaPacket.parse(buffer, fromServer, undefined), packet => {
        if (packet.TAG !== "WorldInfo") {
          return "PacketStructureIsSame";
        }
        let newrecord = {...packet._0};
        return {
          TAG: "ConvertedToLatest",
          _0: {
            TAG: "WorldInfo",
            _0: (newrecord.dungeonY = 0, newrecord.dungeonX = 0, newrecord)
          }
        };
      });
    case "CavernMonsterTypeSync" :
      if (fromServer) {
        return {
          TAG: "Ok",
          _0: "PacketStructureIsSame"
        };
      } else {
        return {
          TAG: "Ok",
          _0: "DiscardAsNotExists"
        };
      }
    default:
      return {
        TAG: "Ok",
        _0: "PacketStructureIsSame"
      };
  }
}

function convertFromLatestIfNeeded(buffer, fromServer) {
  if (buffer.length < 3) {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketLength",
        _0: buffer.length
      }
    };
  }
  let match = PacketType$TerrariaPacket.fromInt(buffer[2]);
  if (match !== undefined) {
    if (match === "WorldInfo") {
      return Stdlib_Result.map(Parser$TerrariaPacket.parse(buffer, fromServer, undefined), packet => {
        if (packet.TAG === "WorldInfo") {
          return {
            TAG: "ConvertedFromLatest",
            _0: {
              TAG: "WorldInfo",
              _0: packet._0
            }
          };
        } else {
          return "PacketStructureIsSame";
        }
      });
    } else {
      return {
        TAG: "Ok",
        _0: "PacketStructureIsSame"
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidPacketType",
        _0: buffer[2]
      }
    };
  }
}

export {
  convertToLatestIfNeeded,
  convertFromLatestIfNeeded,
}
/* Parser-TerrariaPacket Not a pure module */
