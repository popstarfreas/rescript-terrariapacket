// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_int from "@rescript/runtime/lib/es6/Primitive_int.js";
import * as PacketTypeV1456$TerrariaPacket from "../PacketTypeV1456.js";
import * as ErrorAwarePacketReader$TerrariaPacket from "../ErrorAwarePacketReader.js";
import * as ErrorAwarePacketWriter$TerrariaPacket from "../ErrorAwarePacketWriter.js";
import Packetreader from "@popstarfreas/packetfactory/packetreader";

function parse(payload) {
  let reader = new Packetreader(payload);
  let e = ErrorAwarePacketReader$TerrariaPacket.readInt16(reader, "npcId");
  if (e.TAG !== "Ok") {
    return e;
  }
  let buffs = [];
  let buffTimes = [];
  let readBuffs = () => {
    while (true) {
      let e = ErrorAwarePacketReader$TerrariaPacket.readUInt16(reader, "buff");
      if (e.TAG !== "Ok") {
        return e;
      }
      let buff = e._0;
      if (buff === 0) {
        return {
          TAG: "Ok",
          _0: undefined
        };
      }
      let e$1 = ErrorAwarePacketReader$TerrariaPacket.readUInt16(reader, "buffTime");
      if (e$1.TAG !== "Ok") {
        return e$1;
      }
      buffs.push(buff);
      buffTimes.push(e$1._0);
      continue;
    };
  };
  let e$1 = readBuffs();
  if (e$1.TAG === "Ok") {
    return {
      TAG: "Ok",
      _0: {
        npcId: e._0,
        buffs: buffs,
        buffTimes: buffTimes
      }
    };
  } else {
    return e$1;
  }
}

let Decode = {
  readUInt16: ErrorAwarePacketReader$TerrariaPacket.readUInt16,
  readInt16: ErrorAwarePacketReader$TerrariaPacket.readInt16,
  parse: parse
};

function packBuffs(writer, buffs, buffTimes) {
  let len = Primitive_int.min(buffs.length, buffTimes.length);
  for (let i = 0; i < len; ++i) {
    ErrorAwarePacketWriter$TerrariaPacket.packUInt16(writer, buffs[i], `buff` + i.toString());
    ErrorAwarePacketWriter$TerrariaPacket.packUInt16(writer, buffTimes[i], `buffTime` + i.toString());
  }
  return ErrorAwarePacketWriter$TerrariaPacket.packUInt16(writer, 0, "buff_terminator");
}

function toBuffer(self) {
  if (self.buffs.length !== self.buffTimes.length) {
    return {
      TAG: "Error",
      _0: {
        context: "PacketV1456_NpcBuffUpdate.toBuffer",
        error: new Error("Expected buffs and buffTimes to be same length")
      }
    };
  } else {
    return ErrorAwarePacketWriter$TerrariaPacket.data(packBuffs(ErrorAwarePacketWriter$TerrariaPacket.packInt16(ErrorAwarePacketWriter$TerrariaPacket.setType(ErrorAwarePacketWriter$TerrariaPacket.make(), PacketTypeV1456$TerrariaPacket.toInt("NpcBuffUpdate")), self.npcId, "npcId"), self.buffs, self.buffTimes));
  }
}

let Encode = {
  packInt16: ErrorAwarePacketWriter$TerrariaPacket.packInt16,
  packUInt16: ErrorAwarePacketWriter$TerrariaPacket.packUInt16,
  setType: ErrorAwarePacketWriter$TerrariaPacket.setType,
  data: ErrorAwarePacketWriter$TerrariaPacket.data,
  packBuffs: packBuffs,
  toBuffer: toBuffer
};

export {
  Decode,
  Encode,
  parse,
  toBuffer,
}
/* ErrorAwarePacketWriter-TerrariaPacket Not a pure module */
