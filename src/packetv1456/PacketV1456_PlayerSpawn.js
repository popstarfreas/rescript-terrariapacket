// Generated by ReScript, PLEASE EDIT WITH CARE

import * as PacketTypeV1456$TerrariaPacket from "../PacketTypeV1456.js";
import * as ErrorAwarePacketReader$TerrariaPacket from "../ErrorAwarePacketReader.js";
import * as ErrorAwarePacketWriter$TerrariaPacket from "../ErrorAwarePacketWriter.js";
import Packetreader from "@popstarfreas/packetfactory/packetreader";

function parse(payload) {
  let reader = new Packetreader(payload);
  let e = ErrorAwarePacketReader$TerrariaPacket.readByte(reader, "playerId");
  if (e.TAG !== "Ok") {
    return e;
  }
  let e$1 = ErrorAwarePacketReader$TerrariaPacket.readInt16(reader, "x");
  if (e$1.TAG !== "Ok") {
    return e$1;
  }
  let e$2 = ErrorAwarePacketReader$TerrariaPacket.readInt16(reader, "y");
  if (e$2.TAG !== "Ok") {
    return e$2;
  }
  let e$3 = ErrorAwarePacketReader$TerrariaPacket.readInt32(reader, "timeRemaining");
  if (e$3.TAG !== "Ok") {
    return e$3;
  }
  let e$4 = ErrorAwarePacketReader$TerrariaPacket.readInt16(reader, "numberOfDeathsPve");
  if (e$4.TAG !== "Ok") {
    return e$4;
  }
  let e$5 = ErrorAwarePacketReader$TerrariaPacket.readInt16(reader, "numberOfDeathsPvp");
  if (e$5.TAG !== "Ok") {
    return e$5;
  }
  let e$6 = ErrorAwarePacketReader$TerrariaPacket.readByte(reader, "team");
  if (e$6.TAG !== "Ok") {
    return e$6;
  }
  let e$7 = ErrorAwarePacketReader$TerrariaPacket.readByte(reader, "context");
  if (e$7.TAG !== "Ok") {
    return e$7;
  }
  let rawContext = e$7._0;
  let context;
  switch (rawContext) {
    case 0 :
      context = "ReviveFromDeath";
      break;
    case 1 :
      context = "SpawningIntoWorld";
      break;
    case 2 :
      context = "RecallFromItem";
      break;
    case 3 :
      context = "TeamSwap";
      break;
    default:
      context = {
        TAG: "Unknown",
        _0: rawContext
      };
  }
  return {
    TAG: "Ok",
    _0: {
      playerId: e._0,
      x: e$1._0,
      y: e$2._0,
      timeRemaining: e$3._0,
      numberOfDeathsPve: e$4._0,
      numberOfDeathsPvp: e$5._0,
      team: e$6._0,
      context: context
    }
  };
}

let Decode = {
  readByte: ErrorAwarePacketReader$TerrariaPacket.readByte,
  readInt16: ErrorAwarePacketReader$TerrariaPacket.readInt16,
  readInt32: ErrorAwarePacketReader$TerrariaPacket.readInt32,
  parse: parse
};

function toBuffer(self) {
  let n = self.context;
  let tmp;
  if (typeof n !== "object") {
    switch (n) {
      case "ReviveFromDeath" :
        tmp = 0;
        break;
      case "SpawningIntoWorld" :
        tmp = 1;
        break;
      case "RecallFromItem" :
        tmp = 2;
        break;
      case "TeamSwap" :
        tmp = 3;
        break;
    }
  } else {
    tmp = n._0;
  }
  return ErrorAwarePacketWriter$TerrariaPacket.data(ErrorAwarePacketWriter$TerrariaPacket.packByte(ErrorAwarePacketWriter$TerrariaPacket.packByte(ErrorAwarePacketWriter$TerrariaPacket.packInt16(ErrorAwarePacketWriter$TerrariaPacket.packInt16(ErrorAwarePacketWriter$TerrariaPacket.packInt32(ErrorAwarePacketWriter$TerrariaPacket.packInt16(ErrorAwarePacketWriter$TerrariaPacket.packInt16(ErrorAwarePacketWriter$TerrariaPacket.packByte(ErrorAwarePacketWriter$TerrariaPacket.setType(ErrorAwarePacketWriter$TerrariaPacket.make(), PacketTypeV1456$TerrariaPacket.toInt("PlayerSpawn")), self.playerId, "playerId"), self.x, "x"), self.y, "y"), self.timeRemaining, "timeRemaining"), self.numberOfDeathsPve, "numberOfDeathsPve"), self.numberOfDeathsPvp, "numberOfDeathsPvp"), self.team, "team"), tmp, "context"));
}

let Encode = {
  toBuffer: toBuffer
};

export {
  Decode,
  Encode,
  parse,
  toBuffer,
}
/* ErrorAwarePacketWriter-TerrariaPacket Not a pure module */
