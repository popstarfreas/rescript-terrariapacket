// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Zora from "zora";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Packet$TerrariaPacket from "../src/Packet.js";
import * as Parser$TerrariaPacket from "../src/Parser.js";
import * as IParser$TerrariaPacket from "../src/IParser.js";
import * as PacketType$TerrariaPacket from "../src/PacketType.js";
import * as PacketV1456$TerrariaPacket from "../src/PacketV1456.js";
import * as ParserV1456$TerrariaPacket from "../src/ParserV1456.js";
import * as ProjectileKey$TerrariaPacket from "../src/ProjectileKey.js";
import * as PacketTypeV1456$TerrariaPacket from "../src/PacketTypeV1456.js";
import * as ParserConverter$TerrariaPacket from "../src/ParserConverter.js";
import * as PacketV1457_NpcCatch$TerrariaPacket from "../src/packetv1457/PacketV1457_NpcCatch.js";
import * as ParserConverterV1456$TerrariaPacket from "../src/ParserConverterV1456.js";
import * as PacketV1457_ItemOwner$TerrariaPacket from "../src/packetv1457/PacketV1457_ItemOwner.js";
import * as PacketV1457_NpcStrike$TerrariaPacket from "../src/packetv1457/PacketV1457_NpcStrike.js";
import * as PacketV1456_ServerInfo$TerrariaPacket from "../src/packetv1456/PacketV1456_ServerInfo.js";
import * as PacketV1457_ServerInfo$TerrariaPacket from "../src/packetv1457/PacketV1457_ServerInfo.js";
import * as PacketV1457_PlayerDodge$TerrariaPacket from "../src/packetv1457/PacketV1457_PlayerDodge.js";
import * as PacketV1457_DamageNPCAck$TerrariaPacket from "../src/packetv1457/PacketV1457_DamageNPCAck.js";
import * as PacketV1457_PlayerUpdate$TerrariaPacket from "../src/packetv1457/PacketV1457_PlayerUpdate.js";
import * as PacketV1449_NpcItemStrike$TerrariaPacket from "../src/packetv1449/PacketV1449_NpcItemStrike.js";
import * as PacketV1457_NetModuleLoad$TerrariaPacket from "../src/packetv1457/PacketV1457_NetModuleLoad.js";
import * as PacketV1457_ItemDropUpdate$TerrariaPacket from "../src/packetv1457/PacketV1457_ItemDropUpdate.js";
import * as PacketV1457_ProjectileSync$TerrariaPacket from "../src/packetv1457/PacketV1457_ProjectileSync.js";
import * as PacketV1457_ItemOwnerRemove$TerrariaPacket from "../src/packetv1457/PacketV1457_ItemOwnerRemove.js";
import * as PacketV1457_ProjectileDestroy$TerrariaPacket from "../src/packetv1457/PacketV1457_ProjectileDestroy.js";
import * as PacketV1456_PlayerPlatformInfo$TerrariaPacket from "../src/packetv1456/PacketV1456_PlayerPlatformInfo.js";
import * as PacketV1457_PlayerPlatformInfo$TerrariaPacket from "../src/packetv1457/PacketV1457_PlayerPlatformInfo.js";
import * as PacketV1449_ItemDropProtectedUpdate$TerrariaPacket from "../src/packetv1449/PacketV1449_ItemDropProtectedUpdate.js";
import * as PacketV1449_ItemDropShimmeredUpdate$TerrariaPacket from "../src/packetv1449/PacketV1449_ItemDropShimmeredUpdate.js";

function bufferToHex(buffer) {
  return buffer.toString("hex");
}

function packedBuffer(result, t) {
  if (result.TAG === "Ok") {
    return result._0;
  }
  t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(result._0.error), "Packet serialization failed"));
  return Buffer.from("");
}

function expectNotImplemented(result, t) {
  if (result.TAG === "Ok") {
    t.fail("Expected NotImplemented");
    return;
  }
  let error = result._0;
  if (typeof error !== "object" && error === "NotImplemented") {
    t.ok(true);
    return;
  }
  t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error));
}

Zora.test("1.4.5.6 and 1.4.5.7 tail packet IDs remain distinct", t => {
  t.equal(PacketTypeV1456$TerrariaPacket.toInt("ServerInfo"), 162);
  t.equal(PacketTypeV1456$TerrariaPacket.toInt("PlayerPlatformInfo"), 163);
  t.equal(PacketType$TerrariaPacket.toInt("DamageNPCAck"), 162);
  t.equal(PacketType$TerrariaPacket.toInt("ServerInfo"), 163);
  t.equal(PacketType$TerrariaPacket.toInt("PlayerPlatformInfo"), 164);
  let oldServer = packedBuffer(PacketV1456_ServerInfo$TerrariaPacket.toBuffer(), t);
  let oldPlatform = packedBuffer(PacketV1456_PlayerPlatformInfo$TerrariaPacket.toBuffer({
    playerId: 7,
    platformId: "Steam"
  }), t);
  let damageAck = packedBuffer(PacketV1457_DamageNPCAck$TerrariaPacket.toBuffer(), t);
  let server = packedBuffer(PacketV1457_ServerInfo$TerrariaPacket.toBuffer(), t);
  let platform = packedBuffer(PacketV1457_PlayerPlatformInfo$TerrariaPacket.toBuffer({
    playerId: 7,
    platformId: "Steam"
  }), t);
  t.equal(oldServer.toString("hex"), "0300a2");
  t.equal(oldPlatform.toString("hex"), "0500a30706");
  t.equal(damageAck.toString("hex"), "0300a2");
  t.equal(server.toString("hex"), "0300a3");
  t.equal(platform.toString("hex"), "0500a40706");
  let buffer = PacketV1456$TerrariaPacket.toBuffer({
    TAG: "ServerInfo",
    _0: undefined
  }, true);
  if (typeof buffer !== "object") {
    t.fail("Expected v1.4.5.6 aggregate serialization");
  } else if (buffer.TAG === "Ok") {
    t.equal(buffer._0.toString("hex"), "0300a2");
  } else {
    t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(buffer._0.error), "Packet serialization failed"));
  }
  let buffer$1 = Packet$TerrariaPacket.toBuffer({
    TAG: "DamageNPCAck",
    _0: undefined
  }, true);
  if (typeof buffer$1 !== "object") {
    t.fail("Expected DamageNPCAck aggregate serialization");
  } else if (buffer$1.TAG === "Ok") {
    t.equal(buffer$1._0.toString("hex"), "0300a2");
  } else {
    t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(buffer$1._0.error), "Packet serialization failed"));
  }
  let error = Parser$TerrariaPacket.parse(damageAck, true, undefined);
  if (error.TAG === "Ok") {
    if (error._0.TAG === "DamageNPCAck") {
      t.ok(true);
    } else {
      t.fail("Expected DamageNPCAck");
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error._0));
  }
  let packet = ParserV1456$TerrariaPacket.parseLazy(oldServer, true);
  if (packet.TAG === "Ok") {
    t.equal(PacketV1456$TerrariaPacket.LazyPacket.toPacketName(packet._0), "ServerInfo");
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(packet._0));
  }
  let error$1 = ParserConverterV1456$TerrariaPacket.convertToLatestIfNeeded(oldServer, true);
  if (error$1.TAG === "Ok") {
    let match = error$1._0;
    if (typeof match !== "object" || match._0.TAG !== "ServerInfo") {
      t.fail("Expected converted ServerInfo");
    } else {
      t.ok(true);
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$1._0));
  }
  let error$2 = ParserConverterV1456$TerrariaPacket.convertFromLatestIfNeeded(server, true);
  if (error$2.TAG === "Ok") {
    let match$1 = error$2._0;
    if (typeof match$1 !== "object" || match$1._0.TAG !== "ServerInfo") {
      t.fail("Expected ServerInfo converted to v1.4.5.6");
    } else {
      t.ok(true);
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$2._0));
  }
  let error$3 = ParserConverterV1456$TerrariaPacket.convertFromLatestIfNeeded(damageAck, true);
  if (error$3.TAG === "Ok") {
    let tmp = error$3._0;
    if (typeof tmp !== "object") {
      if (tmp === "PacketStructureIsSame") {
        t.fail("Expected DamageNPCAck to be discarded for v1.4.5.6");
        return;
      }
      t.ok(true);
      return;
    } else {
      t.fail("Expected DamageNPCAck to be discarded for v1.4.5.6");
      return;
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$3._0));
    return;
  }
});

Zora.test("1.4.5.7 item reservation fields use 7-bit encoded integers", t => {
  let packet = {
    itemDropId: 4660,
    owner: 5,
    timeToKeepReservation: 300,
    grabDelayPlayer: 9,
    grabDelayTime: 16384,
    position: {
      x: 1.5,
      y: -2.25
    }
  };
  let encoded = packedBuffer(PacketV1457_ItemOwner$TerrariaPacket.toBuffer(packet), t);
  t.equal(encoded.toString("hex"), "140016341205ac02098080010000c03f000010c0");
  let parsed = PacketV1457_ItemOwner$TerrariaPacket.parse(encoded);
  if (parsed.TAG === "Ok") {
    t.equal(parsed._0, packet);
    return;
  }
  t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(parsed._0.error), "Parse failed"));
});

Zora.test("1.4.5.7 projectile keys pack spawner, index and generation", t => {
  let key = {
    spawner: 18,
    index: 341,
    generation: 4660
  };
  let packed = ProjectileKey$TerrariaPacket.toInt(key);
  t.equal(packed, 1221678354);
  t.equal(ProjectileKey$TerrariaPacket.fromInt(packed), key);
  let packet_position = {
    x: 1.5,
    y: -2.25
  };
  let packet = {
    projectileKey: key,
    position: packet_position
  };
  let encoded = packedBuffer(PacketV1457_ProjectileDestroy$TerrariaPacket.toBuffer(packet), t);
  t.equal(encoded.toString("hex"), "0f001d1255d1480000c03f000010c0");
  let parsed = PacketV1457_ProjectileDestroy$TerrariaPacket.parse(encoded);
  if (parsed.TAG === "Ok") {
    t.equal(parsed._0, packet);
  } else {
    t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(parsed._0.error), "Parse failed"));
  }
  let sync_position = {
    x: 1.5,
    y: -2.25
  };
  let sync_velocity = {
    x: 3.25,
    y: -4.5
  };
  let sync_ai = [
    1.25,
    undefined,
    2.5
  ];
  let sync_bannerIdToRespondTo = 500;
  let sync_damage = -123;
  let sync_knockback = 6.75;
  let sync_originalDamage = 456;
  let sync = {
    projectileKey: key,
    position: sync_position,
    velocity: sync_velocity,
    projectileType: 321,
    ai: sync_ai,
    bannerIdToRespondTo: sync_bannerIdToRespondTo,
    damage: sync_damage,
    knockback: sync_knockback,
    originalDamage: sync_originalDamage
  };
  let encodedSync = packedBuffer(PacketV1457_ProjectileSync$TerrariaPacket.toBuffer(sync), t);
  t.equal(encodedSync.toString("hex"), "2d001b1255d1480000c03f000010c000005040000090c041017d010000a03ff40185ff0000d840c80100002040");
  let parsed$1 = PacketV1457_ProjectileSync$TerrariaPacket.parse(encodedSync);
  if (parsed$1.TAG === "Ok") {
    t.equal(parsed$1._0, sync);
    return;
  }
  t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(parsed$1._0.error), "Parse failed"));
});

Zora.test("1.4.5.7 simple changed packets use their new layouts", t => {
  t.equal(packedBuffer(PacketV1457_NpcStrike$TerrariaPacket.toBuffer({
    npcSlotId: 18,
    generation: 52,
    damage: 4660,
    knockback: 1.5,
    direction: 1,
    critical: true
  }), t).toString("hex"), "0d001c123434120000c03f0201");
  t.equal(packedBuffer(PacketV1457_ItemOwnerRemove$TerrariaPacket.toBuffer({
    itemDropId: 1234,
    forceAssignToServer: true
  }), t).toString("hex"), "060027d20401");
  t.equal(packedBuffer(PacketV1457_PlayerDodge$TerrariaPacket.toBuffer({
    playerId: 9,
    dodge: "MysticSash"
  }), t).toString("hex"), "05003e0905");
  t.equal(packedBuffer(PacketV1457_NpcCatch$TerrariaPacket.toBuffer({
    npcId: 1234
  }), t).toString("hex"), "050046d204");
});

Zora.test("1.4.5.7 player update carries dash and snapping-stone flags", t => {
  let packet = {
    playerId: 1,
    control: {
      isHoldingUp: false,
      isHoldingDown: false,
      isHoldingLeft: false,
      isHoldingRight: false,
      isHoldingJump: false,
      isHoldingItemUse: false,
      isHoldingDash: true
    },
    direction: "Left",
    pulleyDirection: undefined,
    vortexStealthActive: false,
    gravityDirection: "Normal",
    shieldRaised: false,
    ghost: false,
    selectedItem: 2,
    position: {
      x: 1.0,
      y: 2.0
    },
    velocity: undefined,
    mountType: undefined,
    potionOfReturn: undefined,
    tryKeepingHoveringUp: false,
    isVoidVaultEnabled: false,
    isSitting: false,
    hasFinishedAnyDd2Event: false,
    isPettingAnimal: false,
    isTheAnimalBeingPetSmall: false,
    tryKeepingHoveringDown: false,
    isSleeping: false,
    autoReuseAllWeapons: false,
    controlDownHold: false,
    isOperatingAnotherEntity: false,
    controlUseTile: false,
    netCameraTarget: undefined,
    lastItemUseAttemptSuccess: false,
    snappingStoneLightUp: true
  };
  let encoded = packedBuffer(PacketV1457_PlayerUpdate$TerrariaPacket.toBuffer(packet), t);
  t.equal(encoded.toString("hex"), "11000d0180100080020000803f00000040");
  let parsed = PacketV1457_PlayerUpdate$TerrariaPacket.parse(encoded);
  if (parsed.TAG === "Ok") {
    t.equal(parsed._0, packet);
    return;
  }
  t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(parsed._0.error), "Parse failed"));
});

Zora.test("1.4.5.7 merges shimmer and protection into item-drop flags", t => {
  let packet = {
    itemDropId: 8,
    position: {
      x: 1.0,
      y: 2.0
    },
    velocity: {
      x: 3.0,
      y: 4.0
    },
    stack: 5,
    prefix: 6,
    ownership: "GrabDelayForAllPlayers",
    itemId: 7,
    shimmer: {
      shimmered: true,
      shimmerTime: 8.5
    },
    enemyGrabDelayTime: 9
  };
  let encoded = packedBuffer(PacketV1457_ItemDropUpdate$TerrariaPacket.toBuffer(packet), t);
  let parsed = PacketV1457_ItemDropUpdate$TerrariaPacket.parse(encoded);
  if (parsed.TAG === "Ok") {
    t.equal(parsed._0, packet);
  } else {
    t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(parsed._0.error), "Parse failed"));
  }
  let creativeUnlock = {
    TAG: "CreativeUnlocks",
    _0: {
      itemId: 1234,
      sacrificeCount: 42
    }
  };
  let encodedCreative = packedBuffer(PacketV1457_NetModuleLoad$TerrariaPacket.toBuffer(creativeUnlock), t);
  t.equal(encodedCreative.toString("hex"), "0900520500d2042a00");
  let parsed$1 = PacketV1457_NetModuleLoad$TerrariaPacket.parse(encodedCreative, true);
  if (parsed$1.TAG === "Ok") {
    t.equal(parsed$1._0, creativeUnlock);
  } else {
    t.fail(Stdlib_Option.getOr(Stdlib_JsExn.message(parsed$1._0.error), "Parse failed"));
  }
  let error = ParserConverterV1456$TerrariaPacket.convertFromLatestIfNeeded(encodedCreative, true);
  if (error.TAG === "Ok") {
    let tmp = error._0;
    if (typeof tmp !== "object") {
      if (tmp === "PacketStructureIsSame") {
        t.fail("Expected the new CreativeUnlocks module to be discarded for v1.4.5.6");
        return;
      }
      t.ok(true);
      return;
    } else {
      t.fail("Expected the new CreativeUnlocks module to be discarded for v1.4.5.6");
      return;
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error._0));
    return;
  }
});

Zora.test("latest parser rejects retired packets while the 1.4.5.6 parser accepts them", t => {
  let npcItemStrike = packedBuffer(PacketV1449_NpcItemStrike$TerrariaPacket.toBuffer({
    npcId: 12,
    playerId: 3
  }), t);
  let shimmeredBuffer = packedBuffer(PacketV1449_ItemDropShimmeredUpdate$TerrariaPacket.toBuffer({
    itemDropId: 1,
    x: 2.0,
    y: 3.0,
    vx: 4.0,
    vy: 5.0,
    stack: 6,
    prefix: 7,
    noDelay: 0,
    itemId: 8,
    shimmered: true,
    shimmeredTime: 9.0
  }), t);
  let protectedBuffer = packedBuffer(PacketV1449_ItemDropProtectedUpdate$TerrariaPacket.toBuffer({
    itemDropId: 10,
    x: 11.0,
    y: 12.0,
    vx: 13.0,
    vy: 14.0,
    stack: 15,
    prefix: 16,
    noDelay: 1,
    itemId: 17,
    timeLeftInWhichTheItemCannotBeTakenByEnemies: 18
  }), t);
  let error = ParserV1456$TerrariaPacket.parse(npcItemStrike, false, undefined);
  if (error.TAG === "Ok") {
    let value = error._0;
    if (value.TAG === "NpcItemStrike") {
      t.equal(value._0.playerId, 3);
    } else {
      t.fail("Expected v1.4.5.6 NpcItemStrike");
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error._0));
  }
  let error$1 = ParserV1456$TerrariaPacket.parse(shimmeredBuffer, true, undefined);
  if (error$1.TAG === "Ok") {
    let value$1 = error$1._0;
    if (value$1.TAG === "ItemDropShimmeredUpdate") {
      t.equal(value$1._0.shimmeredTime, 9.0);
    } else {
      t.fail("Expected v1.4.5.6 ItemDropShimmeredUpdate");
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$1._0));
  }
  let error$2 = ParserV1456$TerrariaPacket.parse(protectedBuffer, true, undefined);
  if (error$2.TAG === "Ok") {
    let value$2 = error$2._0;
    if (value$2.TAG === "ItemDropProtectedUpdate") {
      t.equal(value$2._0.timeLeftInWhichTheItemCannotBeTakenByEnemies, 18);
    } else {
      t.fail("Expected v1.4.5.6 ItemDropProtectedUpdate");
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$2._0));
  }
  expectNotImplemented(Parser$TerrariaPacket.parse(npcItemStrike, false, undefined), t);
  expectNotImplemented(Parser$TerrariaPacket.parse(shimmeredBuffer, true, undefined), t);
  expectNotImplemented(Parser$TerrariaPacket.parse(protectedBuffer, true, undefined), t);
  let error$3 = ParserConverterV1456$TerrariaPacket.convertToLatestIfNeeded(shimmeredBuffer, true);
  if (error$3.TAG === "Ok") {
    let match = error$3._0;
    if (typeof match !== "object") {
      t.fail("Expected merged ItemDropUpdate");
    } else {
      let value$3 = match._0;
      if (value$3.TAG === "ItemDropUpdate") {
        t.equal(value$3._0.shimmer, {
          shimmered: true,
          shimmerTime: 9.0
        });
      } else {
        t.fail("Expected merged ItemDropUpdate");
      }
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$3._0));
  }
  let error$4 = ParserConverter$TerrariaPacket.convertFromV1449IfNeeded(shimmeredBuffer, true);
  if (error$4.TAG === "Ok") {
    let match$1 = error$4._0;
    if (typeof match$1 !== "object") {
      t.fail("Expected v1.4.4.9 item-drop conversion");
    } else {
      let value$4 = match$1._0;
      if (value$4.TAG === "ItemDropUpdate") {
        t.equal(value$4._0.shimmer, {
          shimmered: true,
          shimmerTime: 9.0
        });
      } else {
        t.fail("Expected v1.4.4.9 item-drop conversion");
      }
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$4._0));
  }
  let error$5 = ParserConverter$TerrariaPacket.convertFromV1449IfNeeded(npcItemStrike, false);
  if (error$5.TAG === "Ok") {
    let tmp = error$5._0;
    if (typeof tmp !== "object") {
      if (tmp === "PacketStructureIsSame") {
        t.fail("Expected retired packet to be discarded");
        return;
      }
      t.ok(true);
      return;
    } else {
      t.fail("Expected retired packet to be discarded");
      return;
    }
  } else {
    t.fail(IParser$TerrariaPacket.ParseError.toDisplayString(error$5._0));
    return;
  }
});

export {
  bufferToHex,
  packedBuffer,
  expectNotImplemented,
}
/*  Not a pure module */
